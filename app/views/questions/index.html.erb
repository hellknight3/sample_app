<h1>Questions</h1>
<table>
  <thead>
    <tr>
      <th>description</th>
      
      </tr>
  </thead>

  <tbody>	
    <% @questions.each do |question| %>
      <tr>
	<td><%= question.description%></td>
	<td><%= question.id%></td>
	<td><%= question.exercise_id%></td>
       
        <td><%= link_to 'Destroy', '#' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Question', new_question_path({:exercise_id =>params[:exercise_id] }) %>