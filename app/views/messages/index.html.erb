<h1>Messages</h1>

<table>
<thead>
<th>user name </th>


</thead>
<tbody>
<% @users.each do |user|%>
<% if current_user.id == user.messageable_id%>
	<%@user = User.find(user.user_id)%>
<%else%>
	<%@user = User.find(user.messageable_id)%>
<%end%>
<tr>
	<td><%=@user.name%> </td>
	<td><%= link_to 'New Message', new_message_path({messageable_type: User.name, messageable_id: @user.id}) %></td>
</tr>

<%end%>
	</tbody>
</table>
<%= link_to 'New Conversation', new_message_path({messageable_type: User.name}) %>