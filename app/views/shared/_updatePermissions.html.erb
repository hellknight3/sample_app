<div class="row">
		<div class="col-md-3"></div>
		<div class="col-md-6">
<table class="table">
  <thead>
    <tr>
      <th>Pool name</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
   <tbody>
<% pools.each do |pool| %>
<%@perm = pool.permissions%>
<%unless current_user == nil%>
<%unless is_director?(current_user)%>
	<%pool.permissions.uniq.each do |id|%>

      <tr>
	<td><%=pool.name%></td>
	<td><%@temp = @perm.detect{|w| w.user_id == @user.id} %></td>


	<td>
		<%if @temp == nil%>
			<%=form_for(user) do |f| %> 
			<%= f.hidden_field :pool_id, :value => pool.id %>
			<%= f.hidden_field :func, :value => "addPool" %>
			<%= f.submit "Add to pool", class: "btn btn-success" %>
			<%end%>
		<%else%>
			<%=form_for(user) do |f| %> 
			<%= f.hidden_field :pool_id, :value => pool.id %>
			<%= f.hidden_field :func, :value => "removePool" %>
			<%= f.submit "Remove from pool", class: "btn btn-danger" %>
			<%end%>
		<%end%>
	</td>
	
      </tr>
	  <% end %>
<%else%>
  <tr>
	<td><%=pool.name%></td>
	<td><%@temp = @perm.detect{|w| w.user_id == @user.id} %></td>


	<td>
		<%if @temp == nil%>
			<%=form_for(user) do |f| %> 
			<%= f.hidden_field :pool_id, :value => pool.id %>
			<%= f.hidden_field :func, :value => "addPool" %>
			<%= f.submit "Add to pool", class: "btn btn-success" %>
			<%end%>
		<%else%>
			<%=form_for(user) do |f| %> 
			<%= f.hidden_field :pool_id, :value => pool.id %>
			<%= f.hidden_field :func, :value => "removePool" %>
			<%= f.submit "Remove from pool", class: "btn btn-danger" %>
			<%end%>
		<%end%>
	</td>
	
      </tr>
		<% end %>
	 <% end %>
   <% end %>
    </tbody>
    </table>
	</div>
	<div class="col-md-3"></div>
	</div>
